{
  "name": "@ichiran/grammar",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "devDependencies": {
    "@types/node": "^22.10.2"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && cp -r python dist/python && cp -r data dist/data",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/"
  },
  "sideEffects": false,
  "type": "module",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "zod": "^3.24.1"
  }
}
