{
  "name": "@ichiran/cli",
  "version": "0.0.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/",
    "test:parity": "RUN_PARITY_TESTS=true bun test tests/"
  },
  "bin": {
    "ichiran": "./dist/index.js"
  },
  "dependencies": {
    "@ichiran/core": "workspace:*",
    "@ichiran/node": "workspace:*",
    "commander": "^12.1.0",
    "dotenv": "^17.2.3"
  },
  "sideEffects": false
}
